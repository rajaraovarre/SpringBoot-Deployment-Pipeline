apiVersion: v1
kind: Service
metadata:
  name: wezvatechdemo
  namespace: functional
  labels:
    app: wezvatechdemo
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: wezvatechdemo

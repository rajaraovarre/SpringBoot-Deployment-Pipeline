apiVersion: apps/v1
kind: Deployment
metadata:
  name: wezvatechdemo
  namespace: functional
  labels:
    app: wezvatechdemo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wezvatechdemo
  template:
    metadata:
      labels:
        app: wezvatechdemo
    spec:
      containers:
      - name: wezvatechdemo
        image: adamtravis/democicd:fb4
        ports:
        - containerPort: 8080
